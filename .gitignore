.anchor
.DS_Store
target
**/*.rs.bk
node_modules
dist
test-ledger
.yarn
.surfpool
.gstack/
.vercel

# frontend
app/.env
app/*.env

# backend (Moments / TxLINE)
backend/.env
backend/*.env
backend/devnet-keypair.json
backend/.txline-session.json
backend/dist

# backend admin: curated match metadata + generated match cover images (runtime state,
# not source -- see matchMetadataStore.ts / matchImages.ts)
backend/data
backend/media
